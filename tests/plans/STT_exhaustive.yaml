name: "STT Performance Sweep"
description: "Comparison across all transcription models."
settings:
  purge_on_entry: true
  purge_on_exit: true
  track_prior_vram: true
execution:
  - domain: stt
    loadouts:
      - ["faster-whisper-tiny"]
      - ["faster-whisper-base"]
      - ["faster-whisper-small"]
      - ["faster-whisper-medium"]
      - ["faster-whisper-large"]
