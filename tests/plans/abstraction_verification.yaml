name: "Unified Abstraction Verification"
description: "Tests fixed bindings and mock injection via the new NodeImplementation system."

execution:
  - domain: "core"
    pipeline: "unified_abstraction_check"
    loadouts: [["native://faster-whisper-tiny", "ollama://qwen2.5:0.5b"]]
    mapping:
      proc_stt: "mock:This is a mock transcription from the test plan."
      proc_llm: "mock:This is a mock AI response from the test plan."
    scenarios: [stt_polish_std]
