name: "STT Fast Check"
description: "Quick verification of the STT engine."
settings:
  purge_on_entry: true
  purge_on_exit: true
  track_prior_vram: true
execution:
  - domain: stt
    loadouts:
      - ["faster-whisper-base"]
