id: text_only
description: "Simple text-in, text-out pipeline."
nodes:
  - id: input_text
    type: input
    source: user_interface
    buffer: "buffers/instruction.txt"

  - id: proc_llm
    type: processing
    role: llm
    capabilities: [text_in, text_out]
    inputs: 
      - input_text
